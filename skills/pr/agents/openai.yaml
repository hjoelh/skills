interface:
  display_name: "PR"
  short_description: "Creates a concise GitHub PR."
  default_prompt: "Use $pr to open a concise PR from my current branch into the repo default branch with no test plan."
