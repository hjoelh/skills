interface:
  display_name: "PR"
  short_description: "Creates a concise draft GitHub PR from the current worktree state."
  default_prompt: "Use $pr to create any needed commit, recover from detached HEAD worktrees by creating a branch if needed, and open a concise draft PR into the repo default branch with no test plan."
